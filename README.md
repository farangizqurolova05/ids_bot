If you receive a file from an unknown person on Telegram, check it for your safety. The bot compares the files and shows whether they are dangerous or safe. 
I used https://railway.com to make the bot work 24/7. I used botFather and cod to get the bot token. 
import os
import re
import io
import hashlib
import zip file
import log
import structure
pathlib import Path
import datetime libraries outside telebot.
